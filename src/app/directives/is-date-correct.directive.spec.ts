import { IsDateCorrectDirective } from './is-date-correct.directive';

describe('IsDateCorrectDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDateCorrectDirective();
    expect(directive).toBeTruthy();
  });
});
