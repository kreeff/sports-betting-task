import { IsOddsCorrectDirective } from './is-odds-correct.directive';

describe('IsOddsCorrectDirective', () => {
  it('should create an instance', () => {
    const directive = new IsOddsCorrectDirective();
    expect(directive).toBeTruthy();
  });
});
